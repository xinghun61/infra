application: chopsui-gallery
runtime: go
api_version: go1

handlers:
- url: /static
  static_dir: static

- url: /demo
  static_dir: demo
  secure: always

- url: /elements
  static_dir: elements
  secure: always

- url: /bower_components
  static_dir: bower_components
  secure: always

- url: /.*
  script: _go_app
  secure: always
