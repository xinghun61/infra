The following commands should be done while in the dashboard/ directory


Use gae.py to deploy:
gae.py upload -A chropsdash
gae.py switch -A chropsdash

To run a local instance:
gae.py devserver -A chropsdash
