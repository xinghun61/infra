apiVersion: v1
kind: Pod
metadata:
  name: monitoring-proxy
spec:
  containers:
    - name: monitoring-proxy-go-image
      image: gcr.io/chrome_infra_mon_proxy/monitoring_proxy_go_f47f271c1ff5
      imagePullPolicy: Always
  restartPolicy: Always
  dnsPolicy: Default
